[project]
name = "rpcats"
version = "0.1.0"
description = "Display cat information for the specified breed"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]
